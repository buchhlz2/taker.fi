import React from "react";

const NotFound = () => (
  <div className="main-card">
    <h3 className="center-text">404 PAGE NOT FOUND</h3>
    <p>Sorry, but the page you are looking for does not exist.</p>
  </div>
);

export default NotFound;
